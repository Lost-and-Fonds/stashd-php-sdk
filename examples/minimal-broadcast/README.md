# Minimal Broadcast example

`MinimalBroadcast.php` shows the four methods required by the SDK's
`BroadcastPlugin` interface. A real package adds a `stashd-plugin/` manifest and
entrypoint and declares `stashd/plugin-sdk` as a Composer dependency.
